# Netlify SPA routing configuration
# Simple catch-all: serve index.html for all routes that don't match existing files
# Netlify automatically serves static files if they exist, so we don't need explicit rules

/*    /index.html   200